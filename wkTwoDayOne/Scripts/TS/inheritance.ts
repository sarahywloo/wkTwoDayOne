'use strict';

//Inheritance

/*
abstract class Vehicle {
    year: string;
    make: string;
    model: string;
    constructor(year, make, model) {
        this.year = year;
        this.make = make;
        this.model = model;
    }
    report() {
        console.log(`Year: ${this.year}`);
        console.log(`Make: ${this.make}`);
        console.log(`Model: ${this.model}`);
    }
}

class Car extends Vehicle {
    doors: number;
    type: string;
    constructor(year, make, model, doors, type) {
        super(year, make, model);
        this.doors = doors;
        this.type = type;
    }
    report() {
        super.report();
        console.log(`Doors: ${this.doors}`);
        console.log(`Type: ${this.type}`);
    }

}

class Truck extends Vehicle {
    towingCapacity: number;
    constructor(year, make, model, towingCapacity) {
        super(year, make, model);
        this.towingCapacity = towingCapacity;
    }
    report() {
        super.report();
        console.log(`Towing Capacity: ${this.towingCapacity} units`);
    }
}

let myCar = new Car('2015', 'Toyota', 'Camry', 4, 'Sedan');
let myTruck = new Truck('2016', 'Dodge', 'Ram', 5000);

myCar.report();
myTruck.report();

*/
